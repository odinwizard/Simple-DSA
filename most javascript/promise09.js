//Write a function that logs an error message to the console if a certain condition is not met.

function logErrorIf(condition, message) {
    if (!condition) {
      console.error(message);
    }
  }

//  logErrorIf
// It takes two parameters:
// condition: A boolean (true/false) value that controls whether to log the error.
// message: A string — the error message to show if the condition is false.


